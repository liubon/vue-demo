// 测试环境配置
const testProtocol = 'http://';
const testHost = 'vue-demo-test.com';
const testConfig = {
  host: testHost,
  // port: testPort,
  url: `${testProtocol}${testHost}`,
  apiUrl: `${testProtocol}${testHost}`,
  staticUrl: `${testProtocol}${testHost}`,
};
export default testConfig;
